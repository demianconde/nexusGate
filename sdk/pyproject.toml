[project]
name = "aegisflow-sdk"
version = "0.1.0"
description = "AegisFlow SDK — Python client with circuit breaker, retry, and fail-open"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
]

[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"